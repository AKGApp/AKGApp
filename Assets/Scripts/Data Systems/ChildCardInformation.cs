using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ChildCardInformation : MonoBehaviour
{
    // Start is called before the first frame update
    [Header("Child Data")]
    [SerializeField]
    public ChildInfortmationDataClass Data;
    private void Awake() {
        
    }
}
