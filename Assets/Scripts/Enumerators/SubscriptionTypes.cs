/// <summary>
/// An enumerator that contains the types of subscriptions
/// </summary>
public enum SubscriptionTypes
{
    Free, Paid,
}

// to get the text value use the following code Enum.GetName(typeof(SubscriptionTypes), enumVaraible);